<p id="notice"><%= notice %></p>

<h1>Listing Todos</h1>
<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <thead>
      <tr>
        <th>Todo Item</th>
        <th>Created By</th>
        <th colspan="3"></th>
      </tr>
      </thead>
      <tbody>
      <% @todos.each do |todo| %>
          <tr>
            <td><%= todo.name %></td>
            <td><%= todo.user.name %></td>
            <td><%= link_to 'Show', todo, class: 'btn btn-mini' %></td>
            <td><%= link_to 'Edit', edit_todo_path(todo), class: 'btn btn-mini btn-success' %></td>
            <td><%= link_to 'Destroy', todo, method: :delete, class: 'btn btn-mini btn-danger', data: { confirm: 'Are you sure?' } %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>


<br>

<%= link_to 'New Todo', new_todo_path, class: 'btn btn-primary' %>
